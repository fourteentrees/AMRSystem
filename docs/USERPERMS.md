# User Permissioning
Users have three "roles" (as they would be called on the old system): Requester, Crawl Manager, and Admin. With Django, we can assign permissions more granularly. For instance, a user could be a global reader.

## Assigning a User Ad Crawl Permissions
F